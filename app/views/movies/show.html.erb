<section class="movie-details">
  <div class="details">
    <h1><%= @movie.title %></h1>
    <h2>
      <%= year_of(@movie) %> &bull; <%= @movie.rating %>
    </h2>
    <p>
      <%= @movie.description %>
    </p>
    <table>
      <tr>
        <th>Total Gross:</th>
        <td><%= total_gross(@movie) %></td>
      </tr>
    </table>
    <section class="admin">
      <%= link_to "Edit", edit_movie_path(@movie), class: "button" %>
    </section>
    <section class="navigation">
      <p> <%= link_to 'Back to all Movies', @movie, class: "button" %> </p>
      <p> <%= link_to 'Delete', @movie, class: "button", method: :delete, data: {confirm: 'Are you sure?'} %> </p>
    </section>
  </div>
</section>
